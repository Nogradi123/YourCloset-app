<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/items.css" />
</head>

<h2>Create an Outfit</h2>

<div class="item-feed">
    {{#if theUser}}
        {{#each allItems}}
            <img width="200px" height="220px" src="{{image}}">
            <form action="/outfits/{{_id}}/updated/{{../outfit._id}}" method="post">
                <button>Add Item to Outfit</button>
            </form>
            <br>
            <form action="{{_id}}" method="post">
                <button class="delete-btn">Delete</button>
            </form>
            <br>

        {{/each}}
    {{/if}}


</div>

